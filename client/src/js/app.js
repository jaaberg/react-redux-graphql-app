var React = require('react');

require('../style/common.scss');

var App = React.createClass({
  render: function() {
    return (
      <div>
        <p> Noob </p>
      </div>
    );
  }
});

module.exports = App;
